{% extends 'base.html.twig' %}

{% block title %}Catégories!{% endblock %}

{% block body %}
<style>
   
</style>

<div class="example-wrapper">
    <h1>Catégories</h1>

   
    <ul>
        {% for category in categories %}
        <li>
            <a href="{{ path('categories.show', { slug: category.titre, id: category.id }) }}">{{ category.titre }}</a>
           
        </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}