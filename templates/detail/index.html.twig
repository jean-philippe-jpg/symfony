{% extends 'base.html.twig' %}

{% block title %}Hello ServicesController!{% endblock %}

{% block body %}
<style>
   
</style>

<div class="example-wrapper">
    <h1>Details</h1>

   
    <ul>
        {% for details in detail %}
        <li>
            <a href="{{ path('detail.show', { slug: details.titre, id: details.id }) }}">{{ details.titre }}</a>
        </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
