{% extends 'base.html.twig' %}

{% block title "detail:" ~ slug %}

{% block body %}
<style>
    
</style>

<div class="example-wrapper">
    <h1> Catégories</h1>

   {{  slug  }}
   
</div>

<div class="example-wrapper">
    <h1>services</h1>

    <ul>
        {% for detail in details %}
        <li>
           <a href="{{ path('detail.findOne', { slug: detail.titre, id: detail.id }) }}">{{ detail.titre }}</a>

        </li>
      
      
        {% endfor %}
    </ul>
</div>
{% endblock %}